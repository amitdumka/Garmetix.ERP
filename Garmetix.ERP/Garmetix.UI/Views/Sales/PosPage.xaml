<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:inputs="clr-namespace:Syncfusion.Maui.Inputs;assembly=Syncfusion.Maui.Inputs"
             xmlns:sfgrid="clr-namespace:Syncfusion.Maui.DataGrid;assembly=Syncfusion.Maui.DataGrid"
             x:Class="Garmetix.UI.Views.Sales.PosPage"
             Title="{Binding Title}" BackgroundColor="#F8FAFC">

    <Grid ColumnDefinitions="6.5*, 3.5*" ColumnSpacing="15" Padding="15">
        
        <VerticalStackLayout Grid.Column="0" Spacing="15">
            
            <Border BackgroundColor="White" Padding="15" StrokeShape="RoundRectangle 8" StrokeThickness="0">
                <inputs:SfAutocomplete ItemsSource="{Binding AvailableProducts}" DisplayMemberPath="Name" TextMemberPath="Name"
                                       SelectedItem="{Binding SelectedProduct, Mode=TwoWay}"
                                       Placeholder="🔍 Scan Barcode or Type Product Name..." 
                                       NoResultsFoundText="Product not found" FontAttributes="Bold"/>
            </Border>

            <!--<Border BackgroundColor="White" Padding="10" StrokeShape="RoundRectangle 8" StrokeThickness="0" VerticalOptions="FillAndExpand">
                <sfgrid:SfDataGrid ItemsSource="{Binding CartItems}" AutoGenerateColumnsMode="None" HeaderRowHeight="50" RowHeight="55">
                    <sfgrid:SfDataGrid.Columns>
                        <sfgrid:DataGridTextColumn MappingName="ProductName" HeaderText="Item" Width="200"/>
                        <sfgrid:DataGridNumericColumn MappingName="Rate" HeaderText="Rate" Format="N2"/>
                        <sfgrid:DataGridNumericColumn MappingName="Quantity" HeaderText="Qty" Format="0.##"/>
                        <sfgrid:DataGridNumericColumn MappingName="TotalAmount" HeaderText="Total (₹)" Format="N2" FontAttributes="Bold"/>
                    </sfgrid:SfDataGrid.Columns>
                </sfgrid:SfDataGrid>
            not used
            </Border>-->
            <Border BackgroundColor="White" Padding="10" StrokeShape="RoundRectangle 8" StrokeThickness="0" VerticalOptions="Fill">
            <VerticalStackLayout VerticalOptions="Fill" >  
            <sfgrid:SfDataGrid ItemsSource="{Binding CartItems}"  ColumnWidthMode="Fill"  VerticalOptions="Fill"  AutoGenerateColumnsMode="None" HeaderRowHeight="50" RowHeight="55">
                <sfgrid:SfDataGrid.Columns>
                    <sfgrid:DataGridTextColumn MappingName="ProductName" HeaderText="Item" Width="200"/>
                        <sfgrid:DataGridNumericColumn MappingName="Rate" HeaderText="Rate" Format="N2"/>
                        <sfgrid:DataGridNumericColumn MappingName="Quantity" HeaderText="Qty" Format="0.##"/>

                        <sfgrid:DataGridTemplateColumn MappingName="TotalAmount" HeaderText="Total (₹)">
                            <sfgrid:DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Label Text="{Binding TotalAmount, StringFormat='{0:N2}'}" 
                               FontAttributes="Bold" 
                               VerticalTextAlignment="Center" 
                               HorizontalTextAlignment="End"
                               Padding="10,0"/>
                                </DataTemplate>
                            </sfgrid:DataGridTemplateColumn.CellTemplate>
                        </sfgrid:DataGridTemplateColumn>

                    </sfgrid:SfDataGrid.Columns>
                </sfgrid:SfDataGrid>
            </VerticalStackLayout>
            </Border>
        </VerticalStackLayout>

        <Border Grid.Column="1" BackgroundColor="#0F172A" Padding="20" StrokeShape="RoundRectangle 12" StrokeThickness="0">
            <VerticalStackLayout Spacing="20">
                
                <Label Text="CUSTOMER" FontSize="12" FontAttributes="Bold" TextColor="#94A3B8" CharacterSpacing="2"/>
                <Entry Placeholder="Mobile Number" Text="{Binding CurrentBill.MobileNo}" Keyboard="Numeric" BackgroundColor="#1E293B" TextColor="White"/>
                <Entry Placeholder="Customer Name" Text="{Binding CurrentBill.CustomerName}" BackgroundColor="#1E293B" TextColor="White"/>

                <BoxView HeightRequest="1" Color="#334155" Margin="0,10"/>

                <Label Text="PAYMENT" FontSize="12" FontAttributes="Bold" TextColor="#94A3B8" CharacterSpacing="2"/>
                <inputs:SfComboBox ItemsSource="{Binding PaymentModes}" SelectedItem="{Binding CurrentBill.PaymentMode}" BackgroundColor="#1E293B" TextColor="White"/>

                <BoxView HeightRequest="1" Color="#334155" Margin="0,10"/>

                <Grid ColumnDefinitions="*, Auto" RowDefinitions="Auto, Auto, Auto" RowSpacing="15">
                    <Label Grid.Row="0" Grid.Column="0" Text="Subtotal" TextColor="#CBD5E1" FontSize="16"/>
                    <Label Grid.Row="0" Grid.Column="1" Text="{Binding CurrentBill.SubTotal, StringFormat='₹ {0:N2}'}" TextColor="White" FontSize="16" FontAttributes="Bold"/>

                    <Label Grid.Row="1" Grid.Column="0" Text="Tax (GST)" TextColor="#CBD5E1" FontSize="16"/>
                    <Label Grid.Row="1" Grid.Column="1" Text="{Binding CurrentBill.TotalTax, StringFormat='₹ {0:N2}'}" TextColor="White" FontSize="16" FontAttributes="Bold"/>

                    <Label Grid.Row="2" Grid.Column="0" Text="GRAND TOTAL" TextColor="#D4AF37" FontSize="20" FontAttributes="Bold"/>
                    <Label Grid.Row="2" Grid.Column="1" Text="{Binding CurrentBill.GrandTotal, StringFormat='₹ {0:N2}'}" TextColor="#D4AF37" FontSize="24" FontAttributes="Bold"/>
                </Grid>

                <Button Text="COMPLETE SALE" Command="{Binding CheckoutCommand}" BackgroundColor="#D4AF37" TextColor="#0F172A" HeightRequest="60" FontSize="18" FontAttributes="Bold" Margin="0,20,0,0">
                    <Button.Shadow><Shadow Brush="#D4AF37" Offset="0,4" Radius="10" Opacity="0.5" /></Button.Shadow>
                </Button>
                
                <ActivityIndicator IsRunning="{Binding IsBusy}" IsVisible="{Binding IsBusy}" Color="#D4AF37"/>

            </VerticalStackLayout>
        </Border>

    </Grid>
</ContentPage>